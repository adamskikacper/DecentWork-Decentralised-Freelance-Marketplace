export const submitProposal = async (jobId: string, proposal: any) => {
  return null;
};

export const getMyProposals = async (freelancerId: string) => {
  return null;
};

export const updateProposal = async (proposalId: string, proposal: any) => {
  return null;
};

export const withdrawProposal = async (proposalId: string) => {
  return null;
};