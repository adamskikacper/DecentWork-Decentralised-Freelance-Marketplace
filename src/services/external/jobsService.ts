import type { JobsData, JobsPageConfig } from "@/types/dashboard";

export const getClientJobsExternal = async (): Promise<JobsData> => {
 return {} as JobsData;
};

export const getFreelancerJobsExternal = async (): Promise<JobsData> => {
 return {} as JobsData;
};

export const getJobsDataExternal = async (): Promise<JobsData> => {
 return {} as JobsData;
};

export const getJobsPageConfigExternal = (): JobsPageConfig => {
 return {} as JobsPageConfig;
};
